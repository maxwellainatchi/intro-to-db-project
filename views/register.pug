doctype
<html>
head
    title Register
body
    if error
        div=error
    form#login(method="POST" action="/register"): table
        tr
            td: label(for="username") Username:
            td: input#username(name="username")
        tr
            td: label(for="password") Password:
            td: input#password(type="password" name="password")
        tr
            td: label(for="passwordconfirm") Confirm Password:
            td: input#passwordconfirm(type="password" name="passwordconfirm")
        tr
            td: label(for="name") Name:
            td: input#name(name="name")
        tr
            td: input(type="submit" value="Register")
    div Already have an account? Login #[a(href="/login") here]!

</html>